# settings.yaml
default:
  db_url: 'sqlite:///channels.db'
#  session_string: 'your_session_string'
  session_path: 'session.txt'
  channels:
    - '@amir_ramzali'
    - '@ramzali_eco'
    - '@jaryane_eghtesad'
    - '@mahdirajabi_com'
    - '@CryptoVizArt'
  
  # GitLab Duo License Configuration
  # Uncomment and configure these settings to enable GitLab Duo license validation
  # gitlab_url: 'https://gitlab.com'  # or your GitLab instance URL
  # gitlab_private_token: 'your_gitlab_personal_access_token'
  # gitlab_project_id: 'your_project_id'  # optional, for project-level checks
